import { SourceMapGenerator } from 'source-map';
import { Snapshot } from '../snapshot';

export function transform(snapshot: Snapshot) {
  let source = snapshot.content;
  let offset = 0;

  while (source.length) {}
}
